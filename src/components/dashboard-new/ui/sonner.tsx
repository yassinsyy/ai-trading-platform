"use client";

// import { useTheme } from "next-themes";
// import { Toaster as Sonner, ToasterProps } from "sonner";

const Toaster = ({ ...props }: any) => {
  // const { theme = "system" } = useTheme();

  return (
    <div className="toaster group">
      {/* Sonner component disabled */}
    </div>
  );
};

export { Toaster };
